# maistorage-interview

A technical assessment project for the MAIStorage interview process.


---

## Quick Start

To get the application up and running with all dependencies, ensure you have **Docker** and **Docker Compose** installed, then run:

```bash
docker compose up --build